<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- server 地址由用户在设置页配置，常为局域网 http://（明文流量），自用 app 全局放行 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CollectorApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Collector"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 系统分享接收入口：B 站 / YouTube App 的分享菜单里选「采集字幕」，转发文本给主界面进确认页。
             无 UI 透明跳板（server 才是链接解析权威，本 activity 只搬运 EXTRA_TEXT）。 -->
        <activity
            android:name=".ShareReceiveActivity"
            android:exported="true"
            android:label="@string/share_receiver_label"
            android:noHistory="true"
            android:theme="@style/Theme.Collector.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>
</manifest>
